import 'package:flutter/material.dart';

class AnonimNameView extends StatelessWidget {
  const AnonimNameView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}