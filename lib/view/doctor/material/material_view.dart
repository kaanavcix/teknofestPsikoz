import 'package:flutter/material.dart';

class MaterialViewDoctor extends StatelessWidget {
  const MaterialViewDoctor({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    body: Column(

      children: [

        Row(
          children: [Container(
            
          )],
        )
      ],
    ),
    );
  }
}